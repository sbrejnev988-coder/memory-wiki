name: memory-wiki
version: 1.3.0
description: Native Hermes active-memory wiki vault with SQLite FTS, Memory Diff Before Answer, Write Firewall 2.0/source policy, first-class Preference Priority Layer, sectioned pack_context, mutation log/undo, transactional batches, retrieval evaluation, claim compiler, typed graph/project context, secret firewall, provenance cards, redacted sync bundles, dashboards, recovery, metadata repair and self-healing tools.
